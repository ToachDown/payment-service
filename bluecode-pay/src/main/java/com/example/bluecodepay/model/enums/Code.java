package com.example.bluecodepay.model.enums;

public enum Code {
    ISSUER_NOT_SUPPORTED,
    INVALID_BARCODE,
    INVALID_BRANCH,
    CANCELED_BY_USER,
    FRAUD_DETECTED,
    INSUFFICIENT_FUNDS,
    INVALID_STATE,
    LIMIT_EXCEEDED,
    SYSTEM_FAILURE
}
